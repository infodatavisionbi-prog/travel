[phases.setup]
nixPkgs = ["python311", "postgresql_16.dev", "gcc", "nodejs_20"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install -r requirements.txt",
  "npm ci --omit=dev --prefix ../wa-bridge"
]

[start]
cmd = "bash ./start.sh"